# Gyani Assistant - Requirements
# Install with: pip install -r requirements.txt

# GUI
customtkinter==5.2.2

# Text-to-Speech
pyttsx3==2.90

# Speech Recognition
SpeechRecognition==3.10.1

# Translation
googletrans==3.1.0a0

# OpenAI (new v1.x SDK)
openai==1.30.1

# Web scraping (Temperature.py)
requests==2.31.0
beautifulsoup4==4.12.3

# News (Tell_News.py)
# uses requests (already listed above)

# Jokes
pyjokes==0.8.3

# Spotify / GUI automation (spotify.py)
pyautogui==0.9.54

# Internet speed test
speedtest-cli==2.1.3

# Audio / clap detection
sounddevice==0.4.6
numpy==2.2.5

# Desktop notifications (SetReminder.py)
plyer==2.1.0

# Birthday dates (birthdates.py)
pandas==2.2.2
openpyxl==3.1.2

# System control (systemRelated_info.py - Windows only)
pywin32==306
winshell==0.6

# PyAudio (microphone input for SpeechRecognition)
# Windows:  pip install pyaudio
# Linux:    sudo apt install portaudio19-dev && pip install pyaudio
# macOS:    brew install portaudio && pip install pyaudio
pyaudio==0.2.14